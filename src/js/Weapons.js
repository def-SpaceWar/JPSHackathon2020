class Sword {
  constructor(x, y, w, h, color, images, attack_point, attack_radius, damage) {
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.color = color;
    this.image = images;
    this.attack_point = attack_point;
    this.attack_radius = attack_radius;
    this.damage = damage;
  }
}

class BowAndArrow {
  //
}
