class Weapon {
    constructor(x, y, width, height, color, image)
    this.x = x; 
    this.y = y;
    this.w = width;
    this.h = height;
    this.c = color;
    this.i = image;
    if (this.x ||)
}