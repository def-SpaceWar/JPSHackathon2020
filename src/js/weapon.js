









/* class Weapon extends Player {
    constructor(x, y, width, height, color, image)
    this.x = x; 
    this.y = y;
    this.width = w;
    this.height = h;
    this.color = c;
    this.image = i;
    if (x-1 !== this.x_speed) {
        x += this.x_speed -1
    }
*/